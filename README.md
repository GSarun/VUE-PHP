# VUE-PHP Full-Stack Project

โปรเจกต์นี้เป็นตัวอย่างการสร้างเว็บแอปพลิเคชันแบบ Full-Stack โดยใช้ **Vue.js 3** สำหรับ Frontend และ **PHP** สำหรับ Backend

## ✨ คุณสมบัติ (Features)

- **ระบบยืนยันตัวตนผู้ใช้ (User Authentication)**: หน้าสำหรับ Login และฟังก์ชัน Logout
- **การจัดการสถานะ (State Management)**: ใช้ Pinia ในการจัดเก็บข้อมูลสถานะการ Login ของผู้ใช้
- **การป้องกัน Route (Route Guarding)**: จำกัดการเข้าถึงหน้า Home (`/`) เฉพาะผู้ใช้ที่ Login แล้วเท่านั้น
- **การเชื่อมต่อ API**: Frontend (Vue) ดึงข้อมูลจาก Backend (PHP) โดยใช้ Axios
- **Backend API**: PHP Script สำหรับจัดการการ Login และส่งข้อมูลตัวอย่าง (Posts)

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend
- **Vue 3**: (Composition API with `<script setup>`)
- **Vite**: Build tool สำหรับ Frontend
- **Vue Router**: สำหรับการจัดการ Routing
- **Pinia**: สำหรับ State Management
- **Axios**: สำหรับการส่ง HTTP requests ไปยัง Backend

### Backend
- **PHP**: สร้าง API endpoints แบบง่าย

## 📂 โครงสร้างโปรเจกต์

```
/
├── backend/         # โค้ดฝั่ง PHP (API)
│   └── api/
│       ├── login.php
│       └── test.php
└── frontend/        # โค้ดฝั่ง Vue.js (Client)
    ├── src/
    │   ├── components/
    │   ├── router/
    │   └── stores/
    └── ...
```

## 🚀 วิธีการติดตั้งและรันโปรเจกต์

### 1. Backend (PHP)

คุณต้องมี PHP ติดตั้งในเครื่อง และรัน Web Server ที่สามารถประมวลผลไฟล์ PHP ได้ วิธีที่ง่ายที่สุดคือการใช้ Built-in server ของ PHP

1.  เปิด Terminal และเข้าไปที่โฟลเดอร์ `backend`:
    ```bash
    cd backend
    ```
2.  รัน PHP built-in web server ที่ port `8080` (ซึ่งเป็น port ที่ Frontend เรียกใช้):
    ```bash
    php -S localhost:8080
    ```
    ตอนนี้ Backend API ของคุณพร้อมใช้งานที่ `http://localhost:8080`

### 2. Frontend (Vue.js)

1.  เปิด Terminal อีกหน้าต่างหนึ่ง และเข้าไปที่โฟลเดอร์ `frontend`:
    ```bash
    cd frontend
    ```
2.  ติดตั้ง Dependencies ที่จำเป็น:
    ```bash
    npm install
    ```
3.  รัน Development Server ของ Vite:
    ```bash
    npm run dev
    ```
4.  เปิดเบราว์เซอร์และเข้าไปที่ URL ที่แสดงใน Terminal (โดยปกติคือ `http://localhost:5173`)

### 3. รันโปรเจกต์ด้วย Docker Compose (แนะนำ)

สำหรับผู้ที่ต้องการรันโปรเจกต์ในสภาพแวดล้อมที่แยกออกจากระบบหลัก และจัดการได้ง่ายขึ้น สามารถใช้ Docker Compose ได้

1.  **ติดตั้ง Docker:** ตรวจสอบให้แน่ใจว่าคุณได้ติดตั้ง Docker Desktop (สำหรับ Windows/macOS) หรือ Docker Engine (สำหรับ Linux) แล้ว
2.  **รันโปรเจกต์:**
    เปิด Terminal ที่ Root Directory ของโปรเจกต์ (`VUE-PHP/`) และรันคำสั่ง:
    ```bash
    docker compose up --build
    ```
    *   คำสั่งนี้จะทำการสร้าง Docker Image (ถ้ายังไม่มีหรือมีการเปลี่ยนแปลง) และรัน Container ทั้งหมดที่กำหนดไว้ใน `docker-compose.yml`
    *   **Frontend:** จะสามารถเข้าถึงได้ที่ `http://localhost:5173`
    *   **Backend API:** จะสามารถเข้าถึงได้ที่ `http://localhost:8080`
    *   หากคุณต้องการรันในโหมดเบื้องหลัง (detached mode) ให้เพิ่ม `-d` ต่อท้ายคำสั่ง: `docker compose up -d --build`
3.  **หยุดโปรเจกต์:**
    เมื่อต้องการหยุดการทำงานของ Container ทั้งหมด ให้กด `Ctrl+C` ใน Terminal ที่รัน `docker compose up` หรือเปิด Terminal ใหม่ที่ Root Directory ของโปรเจกต์ และรันคำสั่ง:
    ```bash
    docker compose down
    ```
    หากต้องการลบ Volume ที่สร้างขึ้นมาด้วย (เช่น สำหรับฐานข้อมูล) ให้ใช้ `docker compose down -v`

### การทดสอบ

- เมื่อเข้าสู่หน้าเว็บครั้งแรก คุณจะถูก redirect ไปยังหน้า `/login`
- **Username** และ **Password** ที่ใช้สำหรับทดสอบถูกกำหนดไว้ในไฟล์ `backend/api/login.php`:
  - **Username:** `admin`
  - **Password:** `password123`
- หลังจาก Login สำเร็จ คุณจะถูกพาไปยังหน้า Home ซึ่งจะแสดงข้อมูลที่ดึงมาจาก `api/test.php`
